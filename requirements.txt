# ── Web framework ────────────────────────────────────────────────────────────
fastapi
uvicorn[standard]

# ── Vector database client ───────────────────────────────────────────────────
qdrant-client>=1.9.0

# ── ONNX Runtime (INT8 inference) ────────────────────────────────────────────
# CPU-only runtime – lightweight, works on Android, Windows, ARM, x86.
onnxruntime>=1.17.0

# ── OpenVINO (Intel iGPU / CPU acceleration) ─────────────────────────────────
# Only for Intel CPUs — NOT available on ARM64/Apple Silicon.
# openvino>=2024.0
# onnxruntime-openvino>=1.17.0

# ── ML / Processors ──────────────────────────────────────────────────────────
# transformers is needed for SiglipProcessor (tokeniser + image processor).
# torch and optimum are NOT needed at inference time — see requirements-export.txt.
transformers>=4.38.0
pillow
numpy

# ── Utilities ────────────────────────────────────────────────────────────────
python-multipart       # FastAPI file uploads
pydantic-settings      # settings management
requests               # CLI client HTTP calls
av>=12.0.0             # PyAV: Python bindings for FFmpeg libav*
